.idea/
.vscode/
api/
esm2022/
fesm2022/
node_modules
npm
# README.md and RELEASE.md at the root are copies of src/README.md and
# src/RELEASE.md (src/package.json "postbuild"), and src/ is ignored below.
# Formatting them here only creates drift the next build overwrites — and
# prettier rewrites the `[comment]: <> (START/END OF GITHUB README)` slice
# markers the release Makefile cuts the published README with.
README.md
RELEASE.md
# Generated by the proto codegen, like the api/ and fesm2022/ output above.
index.d.ts
src/

ondewo-proto-compiler

ondewo-nlu-api
package-lock.json
package.json
npm/
dist/
.test-build/
.test-build-examples/
# jest's `coverageDirectory` output (gitignored); `make prettier PRETTIER_WRITE=-w`
# in .husky/pre-commit would otherwise rewrite the generated lcov-report HTML.
coverage/
